services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - DATABASE_URL=sqlite:///./data/club.db
    volumes:
      - club_data:/app/data
    ports:
      - "8000:8000"

volumes:
  club_data:
